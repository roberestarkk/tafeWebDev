var i;

for (i=1;  i<=10;  i++) 	// i is the loop index
{		
	document.writeln("The number is  " + i );  
	/* The writeln() method is identical to the write() method,
	with the addition of writing a newline character after each statement.*/
}
